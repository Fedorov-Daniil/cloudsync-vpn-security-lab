# Security Policy

This public repository is sanitized by design.

Do not add real infrastructure values, credential values, private configuration files, database exports, backup archives, raw logs, customer data, or live subscription links.

If sensitive material is accidentally committed:

1. Revoke or rotate the exposed material immediately.
2. Remove it from history.
3. Review repository access.
4. Add a scanner rule that catches the pattern.
